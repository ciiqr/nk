files:
  - ~/*

idk:
  - |
    me
    you

packages:
  - build-essential
  - git
  - gnupg

hostname: server-idk

---
when:
  - platform not in ['windows']

packages:
  - godot
  - kubectx

groups:
  - docker
  # OR
  - name: docker
    system: true
    is_member: true
---
