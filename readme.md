# nk

configuration management for humans

## setup

-   `./.nk.yml` OR `~/.nk.yml`

```yaml
sources:
    - ~/Projects/config
    - ~/Projects/config-private
plugins:
    - ~/Projects/nk-plugin-files
```

-   `cargo run`
